{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Orderr list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ref</th>
                <th>Datecreated</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('orderr_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ref }}</td>
                <td>{{ entity.dateCreated }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('orderr_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('orderr_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('orderr_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
